# Page 02 — Retention Cohorts

**Filters:** Cohort window (last N months), months_since_signup 0–12

## Visuals

1. **Matrix** with conditional formatting (green→red): rows = `cohort_month`, columns = `months_since_signup`, values = `[Retention Rate]`

Matches screenshot `02_retention_heatmap.png`.

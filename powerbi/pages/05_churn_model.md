# Page 05 — Churn Model Performance

## Visuals

1. Multi-row card: Accuracy, Precision, Recall, F1, ROC-AUC (from `artifacts/model/metrics.json` — enter as measures or static cards)
2. Matrix: Confusion matrix
3. Line: ROC curve (`roc_curve.csv`)
4. Bar: Feature importance
5. Histogram: churn probability by actual class (`holdout_predictions.csv`)

> ML artifacts are not a classic star-schema fact; treat as a dedicated analysis page.
